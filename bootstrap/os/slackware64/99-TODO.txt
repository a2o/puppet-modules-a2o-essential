Things to do at the end of bootstrap procedure:
- mdadm.conf, script generation with loop from /proc/mdstat and  mdadm -D ?
- real kernel installation - can also be done at the end of successful puppet run
